# Use TanStack AI for Agent Chat orchestration and UI state

Agent Chat will be hosted in the Camox API and built with TanStack AI for model orchestration, structured outputs, streaming chat state, and per-tool approval flow, while the Studio frontend uses TanStack AI React directly for the v1 chat UI. Assistant UI was considered for the interface, but deferred to avoid adding a protocol/runtime adapter before the agent behavior is proven.
