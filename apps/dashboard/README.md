# Dashboard

The Camox Dashboard served from `app.camox.ai`. It handles authentication and
account-level, organization-level, and Project-level workflows outside Camox Studio.

The application uses TanStack Start and deploys to Cloudflare Workers.
